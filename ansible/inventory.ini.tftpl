[web]
web_node ansible_host=${web_ip}

[monitoring]
monitoring_node ansible_host=${monitoring_ip}

[servers:children]
web
monitoring

[servers:vars]
ansible_user=ubuntu
ansible_ssh_private_key_file=~/.ssh/id_ed25519